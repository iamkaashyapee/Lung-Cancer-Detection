# Lung Cancer Survival Prediction — End-to-End Project

This project trains a machine learning model to predict `survived` (0/1) from a patient diagnosis dataset.

## Quickstart

```bash
pip install -r requirements.txt
python src/train.py
streamlit run app/streamlit_app.py
